pub enum DamageType {
    Default,
    Magic,
    Physical
}

pub enum MagicType {
    Default,
    Air,
    Earth,
    Fire,
    Water
}

pub enum PhysicalType {
    Default,
    Crushing,
    Piercing,
    Slashing
}
